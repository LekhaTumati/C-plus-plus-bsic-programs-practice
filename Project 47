#include <iostream>
#include <math.h>

using namespace std;

int main()
{
    int a, b, c;
    cout << "Enter a, b, c\n";
    cin >> a >> b >> c;
    int s = (a+b+c)/2;
    double heron = sqrt(s*(s-a)*(s-b)*(s-c));
    cout << "The area of the triangle is " << heron;
    return 0;
}
